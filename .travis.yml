language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gopibabus/simple-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true gopibabus/simple-react-app npm run test -- --coverage
